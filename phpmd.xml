<?xml version="1.0"?>
<ruleset name="petstore"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_xml_schema.xsd">
    <description>petstore — Conduction PHPMD ruleset (single source: conduction/hydra-gates).</description>

    <!-- The rule set this replaced was rule-for-rule identical to the central
         one (verified by diffing the <rule ref> lists), so this swap changes
         which FILE the rules live in and nothing about what they enforce. -->
    <rule ref="vendor/conduction/hydra-gates/quality-config/phpmd.xml"/>
</ruleset>
